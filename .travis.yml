language: cpp

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get update && sudo apt-get upgrade -y

install:
  - sudo apt-get install -y scons

script:
  - scons

after_script:
  - lz4_stream_test
